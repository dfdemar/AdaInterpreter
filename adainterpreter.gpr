project AdaInterpreter is

   package Compiler is
      for Default_Switches ("ada") use ("-g", "-gnat05");
   end Compiler;

   package Builder is
      for Default_Switches ("ada") use ("-g");
   end Builder;

   for Main use ("Main.adb");

end AdaInterpreter;

